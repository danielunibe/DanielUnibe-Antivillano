# Archivos modificados

## Preexistentes al inicio

`index.css`, `src/App.tsx`, `src/config/assets.ts`, Credits, Projects y `useParallaxScroll.ts` ya estaban modificados. Sus cambios se preservaron y extendieron de forma compatible.

## Intervención de estabilización

- Config/tooling: `package.json`, `src/config/assets.ts`.
- Orquestación: `src/App.tsx`, `src/hooks/useParallaxScroll.ts`.
- Mundo/performance: `SandFog`, `ThreeSky`, `GlobalTicker`, `InteractionSystem`.
- Entradas semánticas: `Portal`, West/North/East zones.
- UI: `IntroScreen`, `NotchMusicPlayer`, `index.css`.
- Overlays: Contact, Credits, Loot Map, Projects, Stack y sus componentes de inventario/inspector/3D.
- Documentación: esta carpeta y sus evidencias.

No hubo commit, push, reset, stash, restore, clean, instalación, cambio de rama ni eliminación de elementos no rastreados.

